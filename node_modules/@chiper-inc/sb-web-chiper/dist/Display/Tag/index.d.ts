import { ITag } from "./interface";
declare const listSkins: {
    primary: string;
    secondary: string;
    success: string;
    lightSuccess: string;
    warning: string;
    error: string;
    light: string;
};
type Colors = keyof typeof listSkins;
/**
 * A `Tag` is a label used to categorize, or organize items using keywords that describe them
 *
 * #### Usage:
 *
 * ```tsx
  import { Tag } from "@chiper-inc/sb-web-chiper";

  <Tag text="Label" />
 * ```
 *
 * #### Demo:
 */
export declare function Tag({ skin, text, icon, outlined, className, ...rest }: ITag & {
    /** Control the skin of a component */
    skin?: Colors;
}): import("react/jsx-runtime").JSX.Element;
export declare namespace Tag {
    var defaultProps: {
        skin: "primary";
    };
}
export {};
