import { IPrice } from "./interface";
/**
 * A `Price` is a component that displays a number in price format chiper.
 *
 * #### Usage:
 *
 * ```tsx
  import { Price } from "@chiper-inc/sb-web-chiper";

  <Price country="CO" decimals={0} value={14333} />
 * ```
 *
 * #### Demo:
 */
export declare function Price({ country, value, decimals, color, size, through, show, }: IPrice): import("react/jsx-runtime").JSX.Element | null;
