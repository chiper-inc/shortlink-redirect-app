import { ITextField } from "../TextField/interface";
export interface ITextFieldWithChip extends ITextField {
    /** Indicates chips */
    chips?: string[];
    /** Called when the user changes the chip's value */
    setChips?: (e: any) => void;
    /** Remove the chip whit icon right  */
    removeChip?: (e: any) => void;
}
