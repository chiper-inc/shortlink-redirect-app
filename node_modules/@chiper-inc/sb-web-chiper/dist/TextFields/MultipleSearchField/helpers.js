"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.suggestResults = exports.boldMatchingText = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const boldMatchingText = (text, query) => {
    const index = text.toLowerCase().indexOf(query.toLowerCase());
    if (index < 0) {
        return text;
    }
    const start = text.substring(0, index);
    const end = text.substring(index + query.length);
    const match = text.substring(index, index + query.length);
    return ((0, jsx_runtime_1.jsxs)(jsx_runtime_1.Fragment, { children: [(0, jsx_runtime_1.jsx)("span", Object.assign({ className: "sbw-font-bold" }, { children: start })), (0, jsx_runtime_1.jsx)("span", { children: match }), (0, jsx_runtime_1.jsx)("span", Object.assign({ className: "sbw-font-bold" }, { children: end }))] }));
};
exports.boldMatchingText = boldMatchingText;
const suggestResults = (query, data) => {
    const filteredResults = data.filter((result) => {
        const title = result.title.toLowerCase();
        const lowerQuery = query.toLowerCase();
        return title.includes(lowerQuery);
    });
    return filteredResults;
};
exports.suggestResults = suggestResults;
