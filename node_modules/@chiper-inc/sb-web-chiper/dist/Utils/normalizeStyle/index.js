"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const normalizeStyle = (pixelQuantity) => {
    var _a;
    const SCREEN_WIDTH = (_a = window === null || window === void 0 ? void 0 : window.visualViewport) === null || _a === void 0 ? void 0 : _a.width;
    const scale = (SCREEN_WIDTH || 360) / 360;
    const newSize = Math.round(pixelQuantity * scale);
    if (newSize > pixelQuantity) {
        return `${pixelQuantity}px`;
    }
    return `${newSize}px`;
};
exports.default = normalizeStyle;
