/// <reference types="react" />
import { IChiperButton } from "./interface";
/**
 * `ChiperButton` is an interactive element that allows users to trigger all
 * primary and secondary actions
 *
 * #### Usage:
 *
 * ```tsx
  import { ChiperButton } from "@chiper-inc/sb-web-chiper";

  <ChiperButton text="Label" uiType="primary" icon="Info" />
 * ```
 ***Note**: You can set the ChiperButton dimensions using `className` property. Exampĺe:
 *
 * ```tsx
  import { ChiperButton } from "@chiper-inc/sb-web-chiper";

  <ChiperButton className="sbw-h-[65px] sbw-w-full" />
 * ```
 *
 * #### Demo:
 */
export declare const ChiperButton: import("react").MemoExoticComponent<({ text, uiType, size, icon, disabled, onClick, className, isLoading, ...rest }: IChiperButton) => import("react/jsx-runtime").JSX.Element>;
