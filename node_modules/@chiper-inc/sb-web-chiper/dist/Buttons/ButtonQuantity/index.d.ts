import { IPropsButtonQuantity } from "./interface";
/**
 * A `ButtonQuantity` is a component that allows you to add and subtract quantities.
 *
 * #### Usage:
 *
 * ```tsx
  import { ButtonQuantity } from "@chiper-inc/sb-web-chiper";

  const [quantity, setQuantity] = useState(0)

  const onAction = (
    prevQuantity: number,
    newQuantity: number,
    correctQuantity:number,
    id:string,
    action: "ON_CLICK_TRASH" | "ON_CLICK_MINUS" | "ON_CLICK_PLUS" | "ON_CHANGE_INPUT"
  ) => {
    setQuantity(correctQuantity);
  };

  <ButtonQuantity
    id="C-1"
    quantity={0}
    maxQuantity={100}
    minQuantity={2}
    multipleQuantity={2}
    onAction={onAction}
    mode="tag"
  />
 * ```
 *
 * #### Demo:
 */
export declare function ButtonQuantity(props: IPropsButtonQuantity): import("react/jsx-runtime").JSX.Element;
