import { IQuantitySelector } from "./interface";
/**
 * A `QuantitySelector` is a component that allows you to add and subtract quantities.
 *
 * #### Usage:
 *
 * ```tsx
  import { QuantitySelector } from "@chiper-inc/sb-web-chiper";

  const [quantity, setQuantity] = useState(0)

  <QuantitySelector maxQuantiy={1000} {...{ quantity, setQuantity }} />
 * ```
 *
 * #### Demo:
 */
export declare function QuantitySelector({ maxQuantiy, minQuantity, quantity, setQuantity, }: IQuantitySelector): import("react/jsx-runtime").JSX.Element;
