import { HTMLAttributes } from "react";
import { sizes } from "./props";
export interface SquareData extends HTMLAttributes<HTMLButtonElement> {
    /** Text inside the square */
    text: string;
    /** size of the square */
    size?: keyof typeof sizes;
    /** selected square */
    selected: boolean;
    classname?: string;
}
export interface FrequencySquaresProps {
    squares: SquareData[];
}
