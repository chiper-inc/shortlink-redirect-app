import { IFrequency } from "./interface";
/**
 * `Frequency` is an interactive element that allows you to select the days of the week
 *
 * #### Usage:
 *
 * ```tsx
  import { Frequency } from "@chiper-inc/sb-web-chiper";

  <Frequency text="L" />
 * ```
 *
 * #### Demo:
 */
export declare function Frequency({ text, size, selected, disabled, onClick, className, ...rest }: IFrequency): import("react/jsx-runtime").JSX.Element;
