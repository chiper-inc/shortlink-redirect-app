import { ITypography } from "./interface";
/**
 * `Typography` helps us create hierarchies, organize
 * information, and guide our users through pages.
 *
 * #### Usage:
 *
 * ```tsx
  import { Typography } from "@chiper-inc/sb-web-chiper";

  <Typography bold>
    Torre de Control
  </Typography>

  <Typography variant="h2" bold>
    Torre de Control
  </Typography>

  <Typography variant="body" as="span">
    Torre de Control
  </Typography>
 * ```
 *
 * #### Demo:
 */
export declare function Typography({ children, as, color, bold, variant, "data-testid": testID, }: ITypography): import("react/jsx-runtime").JSX.Element;
export declare namespace Typography {
    var defaultProps: {
        as: null;
        color: "yankeesBlue";
        bold: boolean;
        variant: string;
    };
}
