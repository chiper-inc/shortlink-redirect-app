import { ITypography } from "../interface";
interface IText extends Omit<ITypography, "children"> {
    /**
     * Deprecated: Use `children` instead of `text`.
     * The content to be displayed inside the Title component as a string
     * @deprecated
     */
    text: string;
}
/**
 * @deprecated Use `Typography` instead
 * TODO: Remove this component next release
 */
export declare function TextAndTitle({ text, ...rest }: IText): import("react/jsx-runtime").JSX.Element;
export {};
