import { HTMLAttributes } from "react";
export interface IOption {
    text: string;
    onClick: () => void;
    selected: boolean;
    disable?: boolean;
}
export interface ISegmentedControl extends HTMLAttributes<HTMLDivElement> {
    options: string[];
    controlSelected?: (index: number) => void;
    initialSelected?: number;
    disable?: boolean;
}
