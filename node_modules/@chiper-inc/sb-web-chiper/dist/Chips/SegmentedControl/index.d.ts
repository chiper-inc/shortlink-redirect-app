import { ISegmentedControl } from "./interface";
/**
 * A `SegmentedControl` is a linear set of two or more segments, each of which functions
 * as a button.
 *
 * #### Usage:
 *
 * ```tsx
  import { SegmentedControl } from "@chiper-inc/sb-web-chiper";

  <SegmentedControl options={['Option 1', 'Option 2']} initialSelected={0} />
 * ```
 *
 * #### Demo:
 */
export declare function SegmentedControl({ options, initialSelected, disable, controlSelected, className, ...rest }: ISegmentedControl): import("react/jsx-runtime").JSX.Element;
