import { IDropdownItem, SimpleDropdownType } from "../interface";
export interface IDropdownListItem extends IDropdownItem {
    isSubItem?: boolean;
    type?: SimpleDropdownType | "checkbox";
    isSelected: (itemValue: string) => boolean;
    onClick: (itemValue: string) => void;
}
