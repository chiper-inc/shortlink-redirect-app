import { ReactNode } from "react";
import { IDropdownUi } from "../interface";
export interface IDropdownWrapper extends Required<IDropdownUi> {
    children: ReactNode;
    "data-testid"?: string;
}
