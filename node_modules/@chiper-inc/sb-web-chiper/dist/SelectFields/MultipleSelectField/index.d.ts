/// <reference types="react" />
import { IMultipleSelectField } from "./interface";
/**
 * A `MultipleSelectField` is a field that allows user to select multiple options
 *
 * #### Interface:
 *
 * ```tsx
  interface ISelectOption {
    text: string;
    value: string;
  }
   ```
 *
 * #### Usage:
 *
 * ```tsx
  import { useState } from "react";
  import { MultipleSelectField } from "@chiper-inc/sb-web-chiper";

  const [selectedValues, setSelectedValues] = useState<string[]>([]);

  const handleClick = useCallback((itemValue: string) => {
    setSelectedValues((prev) => (prev.includes(itemValue)
      ? prev.filter((i) => i !== itemValue)
      : [...prev, itemValue]));
  }, []);

  return (
    <MultipleSelectField
      value={value}
      options=[
        {
          text: "option one",
          value: "1"
        },
        {
          text:"option two",
          value: "2"
        }
      ]
      onOptionClick={handleClick}
     />
  );
 * ```
 *
 * #### Demo:
 *
 */
export declare const MultipleSelectField: import("react").MemoExoticComponent<({ onOptionClick, icon, placeholder, disabled, values, helperText, isValid, full, name, options, "data-testid": testID, maxDropdownHeight, }: IMultipleSelectField) => import("react/jsx-runtime").JSX.Element>;
