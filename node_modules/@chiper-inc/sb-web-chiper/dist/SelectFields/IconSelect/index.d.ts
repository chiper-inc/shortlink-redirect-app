import { IiconSelect } from "./interface";
/**
 * The `IconSelect` is a reusable component that provides
 * a Icon button with an associated dropdown menu.

 *
 * #### Usage:
 *
 * ```tsx
  import {IconSelect} from "@chiper-inc/sb-web-chiper"

  const [status, setStatus] = useState("");

  return (
    <div className={CONTAINER_CLASSES}>
      <ComponentItem {...args} setValue={setStatus} actived={!!status} />
    </div>
  );
};
 * ```
 *
 * #### Demo:
 */
export declare function IconSelect({ label, items, className, size, disabled, setValue, actived, iconName, ...rest }: IiconSelect): import("react/jsx-runtime").JSX.Element;
