import { PhosphorIcons } from "../../Theme/Icon";
import { IDatePicker } from "../DatePicker/interface";
export interface IDateField extends IDatePicker {
    /** format to display selected dates. By default is `dd/MM/yyyy` */
    formatToShow?: string;
    name?: string;
    placeholder: string;
    helperText?: string;
    disabled?: boolean;
    isValid?: boolean;
    full?: boolean;
    isReadOnly?: boolean;
    /** By defaults is `CalendarBlank` */
    icon?: keyof typeof PhosphorIcons | "None";
}
