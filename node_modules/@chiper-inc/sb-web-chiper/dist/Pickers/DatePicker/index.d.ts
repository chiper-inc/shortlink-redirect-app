/// <reference types="react" />
import { IDatePicker } from "./interface";
export declare const locales: {
    es: Locale;
    pt: Locale;
    en: Locale;
};
/**
 * Use `DatePicker` to select a date or range date
 *
 * #### Usage:
 *
 * ```tsx
  import { useState, useCallback } from "react";
  import { DatePicker } from "@chiper-inc/sb-web-chiper";

  const [initRange, setInitRange] = useState<Date | undefined>(undefined);
  const [endRange, setEndRange] = useState<Date | undefined>(undefined);

  const [date, setDate] = useState<Date | undefined>(undefined);

  const handleRangeClick = useCallback((init: Date, end?: Date) => {
    setInitRange(init);
    setEndRange(end);
  }, []);

   const handleDateClick = useCallback((clickedDate: Date) => {
    setDate(clickedDate);
  }, []);

  return (
    <DatePicker
      hasRange
      initDate={initRange}
      endDate={endRange}
      onClick={handleRangeClick}
    />

     <DatePicker
      initDate={date}
      onClick={handleDateClick}
      minDate={new Date("2023/04/05")}
      maxDate={new Date("2023/04/25")}
    />
  );
 * ```
 *
 * #### Demo:
 *
 */
export declare const DatePicker: import("react").MemoExoticComponent<({ initDate, endDate, onClick, minDate, maxDate, hasRange, locale, "data-testid": testID, }: IDatePicker) => import("react/jsx-runtime").JSX.Element>;
