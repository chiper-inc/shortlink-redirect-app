import { CSSProperties, HTMLAttributes } from "react";
import { IColors } from "../../Theme/Colors/interface";
export interface IDivider extends HTMLAttributes<HTMLHRElement> {
    /** Set the `width` using CSS properties */
    width?: CSSProperties["width"];
    /** Set the `height` using CSS properties */
    height?: CSSProperties["height"];
    /** Set the `background` color using an object that implements the `IColors` interface */
    background?: IColors;
}
