import { IDivider } from "./interface";
/**
 * A `Divider` is a thin line that separates or groups content in various layouts
 *
 * #### Usage:
 *
 * ```tsx
  import { Divider } from "@chiper-inc/sb-web-chiper";

  <Divider />
 * ```
 *
 * #### Demo:
 */
export declare function Divider({ width, height, background, className, ...rest }: IDivider): import("react/jsx-runtime").JSX.Element;
export declare namespace Divider {
    var defaultProps: {
        background: "gray25";
    };
}
