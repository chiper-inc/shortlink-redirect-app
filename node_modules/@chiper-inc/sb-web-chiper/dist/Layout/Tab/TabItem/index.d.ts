import { ITabItem } from "../interface";
/**
 * `TabItem` is a component used as a child element of the `TabList`
 * component to represent a single tab item. It can be used with any
 * valid child content and accepts no props. The `TabList` component maps
 * over its children and applies any additional props to the `TabItem` components.
 *
 * #### Usage:
 *
 * ```tsx
  import { TabList, TabItem } from "@chiper-inc/sb-web-chiper";

  <TabList className="sbw-bg-white">
    <TabItem
      title="Item 1"
      to="my-route1"
      selected
      actived
    />
    <TabItem
      title="Item 2"
      to="my-route2"
      selected={false}
      actived
    />
  </TabList>
 * ```
 */
export declare function TabItem({ title, iconName, onClick, to, actived, selected, "data-testid": testID, ...rest }: ITabItem): import("react/jsx-runtime").JSX.Element;
