import { HTMLAttributes, JSXElementConstructor, ReactElement } from "react";
import { NavLinkProps } from "react-router-dom";
import { PhosphorIcons } from "../../Theme/Icon";
export interface ITabList {
    /**
     * The `TabItem` element(s) to be displayed in the tab menu.
     * Use the syntax `<ITabItem>` to specify the type of the child component.
     */
    children: ReactElement<ITabItem, string | JSXElementConstructor<ITabItem>> | ReactElement<ITabItem, string | JSXElementConstructor<ITabItem>>[];
    /**
     * A string representing the CSS classes for the `ul` element that wraps the `ITabItem` elements.
     */
    className?: HTMLAttributes<HTMLUListElement>["className"];
    /**
     * An optional prop that specifies the width of each tab item in pixels.
     */
    itemWidth?: number | null;
    /**
     * Optional boolean that allows to center the `ITabItem` elements within the container.
     */
    justify?: boolean;
    /**
     * Optional string representing a `data-testid` attribute for testing purposes.
     */
    "data-testid"?: string;
}
export interface ITabItem extends Omit<NavLinkProps, "to"> {
    /** A required string prop that represents the title of the tab item. */
    title: string;
    /** Optional string that specifies the URL to link to when the tab is clicked. */
    to?: string;
    /** Optional boolean that specifies if the tab item is currently active. */
    actived?: boolean;
    /** Optional boolean that specifies if the tab item is currently selected. */
    selected?: boolean;
    /** Optional `PhosphorIcons` icon name. */
    iconName?: keyof typeof PhosphorIcons;
    /** Optional string representing a `data-testid` attribute for testing purposes. */
    "data-testid"?: string;
}
