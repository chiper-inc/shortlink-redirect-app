import { IAvatar } from "./interface";
/**
 * `Avatar` is used to give users a sense of identity and individuality within the digital space
 *
 * #### Usage:
 *
 * ```tsx
  import { Avatar } from "@chiper-inc/sb-web-chiper";

  <Avatar type="icon" />
 * ```
 *
 * #### Demo:
 */
export declare function Avatar({ imgSrc, letter, type, size, background, color, icon, className, ...rest }: IAvatar): import("react/jsx-runtime").JSX.Element;
export declare namespace Avatar {
    var defaultProps: {
        size: string;
        background: string;
        color: string;
        icon: string;
    };
}
