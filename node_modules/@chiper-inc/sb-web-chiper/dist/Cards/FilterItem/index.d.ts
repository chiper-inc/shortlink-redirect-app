/// <reference types="react" />
import { IFilterItem } from "./interface";
/**
 * A `FilterItem` is a component that in a row shows the information of a product added to the cart.
 *
 * #### Usage:
 *
 * ```tsx
  import { FilterItem } from "@chiper-inc/sb-web-chiper";

  <FilterItem
    icon= "Storefront"
    title="Factura con anticipo de IVA"
    onClick={() => null}
  />
 * ```
 *
 * #### Demo:
 */
export declare const FilterItem: import("react").MemoExoticComponent<({ icon, title, subTitle, selected, onClick, extraData, currencyExtraData, }: IFilterItem) => import("react/jsx-runtime").JSX.Element>;
