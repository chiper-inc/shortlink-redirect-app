import { IProductCardBasic } from "./interface";
/**
 * A `Product Card Basic` is a component that displays a image and price of a product.
 *
 * #### Usage:
 *
 * ```tsx
  import { ProductCardBasic } from "@chiper-inc/sb-web-chiper";

  <ProductCardBasic img="..." value={12400} onClick{()=>{}}>
    <ButtonQuantity />
  </ProductCardBasic>
 * ```
 *
 * #### Demo:
 */
export declare function ProductCardBasic({ pathImg, price, onClick, children, }: IProductCardBasic): import("react/jsx-runtime").JSX.Element;
