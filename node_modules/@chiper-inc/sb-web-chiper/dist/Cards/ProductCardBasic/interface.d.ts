import { ReactNode } from "react";
export interface IProductCardBasic {
    pathImg: string;
    price: JSX.Element | ReactNode;
    onClick?: () => void;
    children?: JSX.Element | JSX.Element[] | ReactNode;
}
