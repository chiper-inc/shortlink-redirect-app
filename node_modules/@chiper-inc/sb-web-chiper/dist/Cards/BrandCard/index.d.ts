/// <reference types="react" />
import { IBrandCard } from "./interface";
/**
 * A `BrandCard` show information container for displaying content of brand
 *
 * #### Usage:
 *
 * ```tsx
  import { BrandCard } from "@chiper-inc/sb-web-chiper";

  <BrandCard
    id={id}
    image={image}
    brandName={brandName}
    onClickBrand={onClickBrand}
    products={[]}
  />
 * ```
 *
 * #### Demo:
 */
export declare const BrandCard: import("react").MemoExoticComponent<({ id, image, brandName, onClickBrand, products, adTitle, isAd, indexPosition, isLoading, }: IBrandCard) => import("react/jsx-runtime").JSX.Element>;
