/// <reference types="react" />
import { IProductRow } from "./interface";
/**
 * A `ProductRow` is a component that in a row shows the information of a product added to the cart.
 *
 * #### Usage:
 *
 * ```tsx
  import { ProductRow } from "@chiper-inc/sb-web-chiper";

   <ProductRow
    quantity={12}
    oldPrice={43}
    detail="Arroz Blanco Vitamor Diana 500 gr"
    currency= "$"
    price= {19.250}
   />
 * ```
 *
 * #### Demo:
 */
export declare const ProductRow: import("react").MemoExoticComponent<({ quantity, oldQuantity, detail, currency, price, disabled, hasChanged, }: IProductRow) => import("react/jsx-runtime").JSX.Element>;
