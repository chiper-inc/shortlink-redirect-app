/// <reference types="react" />
import { IStepper } from "./interface";
/**
 * A `Stepper` display progress through a sequence of logical and numbered steps.
 *
 * #### Usage:
 *
 * ```tsx
  import { Stepper } from "@chiper-inc/sb-web-chiper";

  export const Component = () => {
    const [activeStep, setActiveStep] = useState(0)
    const steps = [
      "Subir archivo", "Asociar ordenes de compra", "Actualización",
    ];

    return (
      <Stepper
        activeStep={activeStep}
        steps={steps}
      />
    );
  };
 * ```
 *
 * #### Demo:
 */
export declare const Stepper: import("react").MemoExoticComponent<({ steps, activeStep }: IStepper) => import("react/jsx-runtime").JSX.Element>;
