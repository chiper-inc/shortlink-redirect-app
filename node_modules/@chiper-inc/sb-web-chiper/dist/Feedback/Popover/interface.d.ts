import { ReactNode } from "react";
export type PopoverPosition = "up" | "down" | "left" | "right";
export type Variant = "light" | "dark";
export interface IPopover {
    /** Trigger element for the popover */
    children: ReactNode;
    /** Popover’s description's elements to be displayed */
    description: string | string[] | ReactNode | ReactNode[];
    /** Select popover position in relation to its trigger element */
    position?: PopoverPosition;
    /** The `dark` and `light` variant are for information only */
    variant?: Variant;
}
