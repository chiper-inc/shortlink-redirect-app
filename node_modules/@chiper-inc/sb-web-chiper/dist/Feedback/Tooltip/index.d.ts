import { ITooltip } from "./interface";
/**
 * A `Tooltip` is a floating message that clarifies some information
 *
 * #### Usage:
 *
 * ```tsx
  import { Tooltip } from "@chiper-inc/sb-web-chiper";

  <Tooltip
    title={'Tooltip title'}
    description={'Tooltip description'}
    position={'down'}
  >
    <span>Tooltip Example</span>
  </Tooltip>
 * ```
 *
 * #### Demo:
 */
export declare function Tooltip({ children, description, position, title, close, tag, titleClass, tagClass, modeDark, }: ITooltip): import("react/jsx-runtime").JSX.Element;
