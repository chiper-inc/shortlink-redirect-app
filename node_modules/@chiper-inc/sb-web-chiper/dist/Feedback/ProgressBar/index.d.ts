import { IProgressBar } from "./interface";
/**
 * ProgressBar
 *
 * #### Usage:
 *
 * ```tsx
  const [completed, setCompleted] = useState(0);

  useEffect(() => {
    setInterval(() => setCompleted(Math.floor(Math.random() * 100) + 1), 2000);
  }, []);

  <ProgressBar text="Actualizando..." value={completed} />
 * ```
 *
 * #### Demo:
 */
export declare function ProgressBar({ title, value, "data-testid": testID, }: IProgressBar): import("react/jsx-runtime").JSX.Element;
