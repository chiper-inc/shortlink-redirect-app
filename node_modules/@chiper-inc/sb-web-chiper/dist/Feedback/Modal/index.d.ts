import { IModal } from "./interface";
/**
 * A `Modal` is a component that can contain critical information,
 * require decisions, or involve multiple tasks.
 *
 * #### Usage:
 *
 * ```tsx
  import { Modal } from "@chiper-inc/sb-web-chiper";

  const [show, setShow] = useState(false);

  <Button
    text="Open"
    onClick={() => setShow(true)}
    skin="gray"
    outlined
  />

  <Modal
    title="Modal title"
    show={show}
    setShow={setShow}
  >
    <div>Modal content</div>
  </Modal>
 * ```
 *
 * #### Demo:
 */
export declare function Modal({ children, show, setShow, config, title, }: IModal): import("react/jsx-runtime").JSX.Element | null;
export declare namespace Modal {
    var defaultProps: {
        config: {
            positionX: string;
            positionY: string;
            width: string;
            height: string;
            showOverlay: boolean;
        };
    };
}
